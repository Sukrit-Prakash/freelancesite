export const metadata = {
    title: "Portfolio",
    description:
        "Browse BloomScript Technologies portfolio of 6+ shipped mobile apps built with React Native and Flutter — e-commerce, fintech, telemedicine, fitness, and more.",
};

export default function PortfolioLayout({ children }) {
    return children;
}
