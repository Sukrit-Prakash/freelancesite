export const metadata = {
    title: "Services",
    description:
        "Expert React Native and Flutter development services, full-stack mobile solutions, App Store & Play Store deployment, and transparent pricing packages.",
};

export default function ServicesLayout({ children }) {
    return children;
}
