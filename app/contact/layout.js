export const metadata = {
    title: "Contact",
    description:
        "Get in touch to discuss your mobile app project. Free 30-minute consultation calls available. We respond within 24 hours.",
};

export default function ContactLayout({ children }) {
    return children;
}
